using System;

namespace GameData {
    public enum AreaConnectorCode : int {
        Door,
        Window,
        Street,
        Path,
        Tunnel,
        Stairs,
        Extension,
        Contains
    }
}
